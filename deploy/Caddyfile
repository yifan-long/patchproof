# 把 YOUR_DOMAIN 换成你的域名（无域名先用 duckdns 免费动态域名）
YOUR_DOMAIN {
    root * /srv/www
    file_server
    encode gzip

    handle /api/* {
        uri strip_prefix /api
        reverse_proxy backend:8010
    }
}
