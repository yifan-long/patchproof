# PatchProof validation fixture

Small, PatchProof-owned repository used as the safe default target. It has no
network or service dependencies. Its email normalization is intentionally
broken so the required check fails before repair.
